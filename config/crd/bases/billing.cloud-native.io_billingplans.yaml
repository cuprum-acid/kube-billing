---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: billingplans.billing.cloud-native.io
spec:
  group: billing.cloud-native.io
  names:
    kind: BillingPlan
    listKind: BillingPlanList
    plural: billingplans
    singular: billingplan
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BillingPlan is the Schema for the billingplans API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of BillingPlan
            properties:
              billingPeriod:
                description: BillingPeriod is the billing period (e.g., hourly, daily,
                  weekly, monthly, yearly, or custom like 30d)
                type: string
              currency:
                description: Currency is the currency code (e.g., USD, EUR, RUB)
                enum:
                - USD
                - EUR
                - RUB
                - KZT
                type: string
              limits:
                additionalProperties:
                  type: integer
                description: Limits define resource limits for this billing plan
                type: object
              price:
                description: |-
                  foo is an example field of BillingPlan. Edit billingplan_types.go to remove/update
                  Foo *string `json:"foo,omitempty"`
                pattern: ^\d+(\.\d{1,2})?$
                type: string
              requeueIntervalSeconds:
                default: 30
                description: |-
                  RequeueIntervalSeconds is the interval between billing cycles in seconds.
                  Default is 30 seconds for testing. Set to 2592000 for monthly billing (30 days).
                minimum: 1
                type: integer
            required:
            - billingPeriod
            - currency
            type: object
          status:
            description: status defines the observed state of BillingPlan
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
